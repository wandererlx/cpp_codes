cmake_minimum_required(VERSION 3.16)
project(CPP_design_parttern)

set(CMAKE_CXX_STANDARD 17)

include_directories(adapter-pattern)
include_directories(command-pattern)
include_directories(composite-pattern)
include_directories(decorator-pattern)
include_directories(facade-pattern)
include_directories(factory-pattern)

include_directories(iterator-pattern)

include_directories(observer-pattern)

include_directories(singleton-pattern)
include_directories(strategy-pattern)

include_directories(template-method-pattern)